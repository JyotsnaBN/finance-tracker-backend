name: Send Scheduled Finance Reports (Prod)

on:
  schedule:
    - cron: "0 6 1,16 * *"   # 06:00 UTC on the 1st and 16th of every month
  workflow_dispatch:

jobs:
  send-reports-prod:
    runs-on: ubuntu-latest

    steps:
      - name: Login and obtain JWT
        id: auth
        env:
          LOGIN_EMAIL: ${{ secrets.REPORT_USER_EMAIL }}
          LOGIN_PASSWORD: ${{ secrets.REPORT_USER_PASSWORD }}
        run: |
          BODY=$(jq -n --arg e "$LOGIN_EMAIL" --arg p "$LOGIN_PASSWORD" \
            '{"email":$e,"password":$p}')
          RESPONSE=$(curl --show-error --silent \
            -X POST "${{ secrets.PROD_BACKEND_BASE_URL }}/api/auth/login" \
            -H "Content-Type: application/json" \
            -d "$BODY")
          TOKEN=$(echo "$RESPONSE" | grep -o '"token":"[^"]*"' | cut -d'"' -f4)
          if [ -z "$TOKEN" ]; then
            echo "Login failed. Response: $RESPONSE"
            exit 1
          fi
          echo "token=$TOKEN" >> "$GITHUB_OUTPUT"

      - name: Compute date range
        id: dates
        run: |
          DAY=$(date -u +%-d)
          YEAR=$(date -u +%Y)
          MONTH=$(date -u +%m)

          if [ "$DAY" -le 15 ]; then
            # 1st of month: full previous month
            START=$(date -u -d "${YEAR}-${MONTH}-01 -1 month" +%Y-%m-01)
            END=$(date -u -d "${YEAR}-${MONTH}-01 -1 day" +%Y-%m-%d)
          else
            # 16th of month: 1st to 15th of current month
            START="${YEAR}-${MONTH}-01"
            END="${YEAR}-${MONTH}-15"
          fi

          echo "start=$START" >> "$GITHUB_OUTPUT"
          echo "end=$END"     >> "$GITHUB_OUTPUT"

      - name: Send Summary report
        run: |
          curl --fail --show-error --silent \
            -X GET "${{ secrets.PROD_BACKEND_BASE_URL }}/api/reports/summary?startDate=${{ steps.dates.outputs.start }}&endDate=${{ steps.dates.outputs.end }}&sendEmail=true" \
            -H "Authorization: Bearer ${{ steps.auth.outputs.token }}"

      - name: Send Income/Expense report
        run: |
          curl --fail --show-error --silent \
            -X GET "${{ secrets.PROD_BACKEND_BASE_URL }}/api/reports/income-expense?startDate=${{ steps.dates.outputs.start }}&endDate=${{ steps.dates.outputs.end }}&sendEmail=true" \
            -H "Authorization: Bearer ${{ steps.auth.outputs.token }}"

      - name: Send Category Breakdown report
        run: |
          curl --fail --show-error --silent \
            -X GET "${{ secrets.PROD_BACKEND_BASE_URL }}/api/reports/category-breakdown?startDate=${{ steps.dates.outputs.start }}&endDate=${{ steps.dates.outputs.end }}&sendEmail=true" \
            -H "Authorization: Bearer ${{ steps.auth.outputs.token }}"

      - name: Send Account Balances report
        run: |
          curl --fail --show-error --silent \
            -X GET "${{ secrets.PROD_BACKEND_BASE_URL }}/api/reports/account-balances?sendEmail=true" \
            -H "Authorization: Bearer ${{ steps.auth.outputs.token }}"
